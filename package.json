{
  "name": "goat-game-server",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "elysia": "^1.2.0",
    "@elysiajs/cors": "^1.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}
